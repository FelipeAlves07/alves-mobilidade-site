export type {
  LeadStatus, LeadType, Lead, LeadForm, Status,
} from "@/domain/lead/types";

export type {
  TripStatus, Trip, TripForm, QuoteResult, QuoteType, QuoteForm,
} from "@/domain/trip/types";

export type {
  ProposalStatus, Proposal, ProposalForm,
} from "@/domain/proposal/types";

export type {
  FinanceType, FinanceEntry, FinanceEntryForm,
} from "@/domain/finance/types";

export type {
  ReferralStatus, Referral, ReferralForm,
} from "@/domain/referral/types";

export type {
  MessageKey, MarketingSuggestion,
} from "@/domain/marketing/types";

export type {
  DashboardStats, MenuItem,
} from "@/domain/shared/types";
